// Inference

let name = 'Aditya';
let cup = Math.random() > 0.5 ? 10 : 5;

// Annotation
let channelName:string = 'My-Typescript';

// channelName = 95; 
// here it will throw error : Type 'number' is not assignable to type 'string'

let hight = 100;
let tooHigh : boolean = hight > 50 ? true : false;